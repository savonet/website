import Search from '@site/src/components/ApiSearch';

// Replaces the theme's Algolia search box with our own, which searches the same index
// alongside the function reference. Docusaurus renders this wherever a search bar goes,
// so there is exactly one of them in the navbar.
export default Search;
